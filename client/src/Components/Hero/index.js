//imports
import "./Hero.css";

const Hero = (params) => {
    return (
        <div {...params} id="hero">
            <div className="hero-wrapper">test</div>
        </div>
    );
};
export default Hero;
